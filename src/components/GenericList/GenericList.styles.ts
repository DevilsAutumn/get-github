import { Paper } from '@mui/material';
import { styled } from '@mui/system';

export const GenericListWrapper = styled(Paper)({
  margin: '0.5rem 0',
  paddingTop: '1rem',
  paddingBottom: '1rem',
  maxHeight: '24rem',
  overflow: 'auto',
  backgroundColor: 'transparent',
  boxShadow: 'none',
});
